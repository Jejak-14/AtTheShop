<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.attheshop">


    //Internet Acces permission//
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AtTheShop"
        android:usesCleartextTraffic="true">

        <!--Clear text traffic - Webservice calls api<28+ -->
        <activity android:name=".RecycleView"></activity>
        <activity android:name=".FragmentView" />
        <activity android:name=".PropositionActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SupportActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>